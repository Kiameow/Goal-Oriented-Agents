{{!
  dailyPlanning.hbs
  Variables: 
    Commonset
    agentName
    goal
    role
    datetime
    task
}}
---
{{commonset}}

{{ agentName }}'s long-term goal is {{ goal }}, who assumes the role of {{ role }}.
---
Today is day {{datetime}}. {{agentName}} wakes at {{wakeHour}}am. {{#if task}} {{agentName}}'s today's mission is {{ task }}. {{/if}} Output the {{agentName}}'s plan today in broad-strokes according to the prompt above. The output should be a list with serial number where each item are in the form of "<Number>.<StartTime> to <EndTime>:<Plan>". Output multiple plans until {{agentName}} get rest. Do not output anything else. Be sure to include the <##FLAG##> sign.
Example output:
"output": "<##FLAG##>1. 9'o clock to 10'o clock: Wake up and complete the morning routine (brush teeth, shower, get dressed). 2. 10'o clock to 14'o clock: Attend college classes.<##FLAG##>"